const sum = (a, b) => a + b
const multiplication = (a, b) => a * b
const division = (a, b) => a / b
const subtraction = (a, b) => a - b

let name = 'Minha calculadora v1.0'

module.exports = {
  sum,
  multiplication,
  division,
  subtraction,
  name
}
